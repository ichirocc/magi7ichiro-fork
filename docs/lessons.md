# 教訓メモ

**要約**：作る前に前提を測ると無駄が消え、検証の穴（UI 層はホストでコンパイルできない・検証器自体が空振りする・断定は観測を超えやすい）が事故の大半を生む。測る対象にはコンテキストの固定費も含める（§6）。

このファイルは**追記でなく更新**する。新しい教訓が出たら該当行を書き換える／表に1行足す。
同じ趣旨の行が増えたら統合する。日付でなく「何をやめて何を続けるか」を残す。

最終更新：2026-08-19（3.321.0〜3.409.4 の作業から）

---

## 1. 修正した点 ↔ 機能した点（対で読む）

| # | 修正した点（やってしまったこと） | 機能した点（次も続けること） |
|---|---|---|
| 1 | **アンカー行の一致だけで挿入位置を決めた**。同じ誤りを2度踏んだ: (a)`val mappedDiag = ...` は `analyzeParallel` と `makeUi` の2箇所にあり最初の一致（別の関数）に入った（3.324.1） (b)複数行の呼び出し `val rBlockSwap = applyAdaptiveBlockSwapPolish(` の**次の行**へ挿入し、引数リストの途中に文が入った（3.326.0） | 挿入は**アンカーが「文の終わり」かを確かめてから**行う。同名行が複数あるファイルでは置換アンカーに関数境界を含める。編集後は**宣言行と使用行が同じ関数に属するか**を行番号から逆引きして検査する（括弧均衡は 0 でも通ってしまう） |
| 2 | **UI へ運ぶ値を、UI を作る関数より後で代入**した。`pushReport`（`makeUi` を呼ぶ唯一の経路）のあとに診断を代入し、その回の画面に出なかった（3.322.0→3.324.0） | 値の生産と消費の**順序を経路で確認する**（「makeUi を呼ぶのはどこか」を先に grep する） |
| 3 | **採用しなかった盤面の診断を保存**し、かつ**無効化する経路を作らなかった**。手編集後も古い観測が出続けた（3.322.0/3.323.0→3.324.0） | 再計算できない値は**どの入力に対する観測か**を持たせる。盤面の内容ハッシュで自己失効させる方式にした（変更サイトごとにフックを足すと必ず漏れる） |
| 4 | **件数の意味を強く書いた**。`pinBlocked` を「全パス横断・手の数」と書いたが、実際は9パスのみ・試行回数・最大4巡を重複排除なし（3.323.0→3.324.0） | 集計を出すときは**母集団と単位を先に確認**する。言えるのは「少なくとも N 回」までだった |
| 5 | **「0 なら緩めても何も変わらない」と断定**した。未計測のパスがあるので証明にならない（3.323.0→3.324.0） | 観測ベースの診断は**観測の範囲を超えない**。「構造的に不能」「すべて」「必ず」を使う前に、その主張を反証できる手が探索側に無いか確かめる |
| 6 | **緩め幅を決め打ち**して「1回ぶん緩めると通る」と書いた。実測では ±1 と ±3 の優劣がデータで逆転した（3.323.0→3.324.0） | 幅・閾値は**示さず判断材料だけ出す**。パラメータを動かすのは業務判断（HF77） |
| 7 | **件数の多数決で原因を分類**し、「スコア却下8・候補なし10」を「入れ替え相手が見つかりません」と案内した。実際は相手が居て禁止連続で落ちていた（3.322.0 初版） | 強い主張は**1件の反例で崩れる**。「候補なし」は候補が1件でも作れていたら名乗らない規則にした |
| 8 | **一つのパスの測定結果を全体へ一般化**した。「ピン破りが不採用の73〜100%」は Apt/Fair/C3系の話で、C1 自身は 0〜1件・主因は `low` だった（3.321.0→3.322.0） | 測った対象を**名前で限定して書く**。同じ設定を指していても、どちらのガードが先に発火するかでラベルが変わる |
| 9 | **原因の仮説を測る前に信じた**。exactWindow が諦める理由を「関与職員集合の選抜」と考えたが、測ったら別の場所（探索の目的関数）だった（3.314.0/3.315.0）。同型: レビューの「Block Swap が300秒を超える」を信じかけたが、測ると 18〜70ms で時間の問題ではなかった（3.327.0） | 疑った箇所を**先に計測して否定する**。仮説が外れたことを結論として記録する。**受け取った指摘の「重大度」も指摘の「事実」とは別に検証する**（食い違いは実在しても、理由が実在するとは限らない） |
| 10 | **数の意味論を曖昧にしたまま数字を出した**。`pinBroken` は `isBetter` が偽でも計上しており、意味を「採用が認められた手をピンだけが止めた回数」へ厳密化したら実データで **543→23** に激減した＝前に出した数は過大だった（3.323.0→3.326.0） | 数字を画面へ出す前に**「この数は何を数えているか」を1文で書けるか**確かめる。書けないなら意味論が決まっていない |
| 11 | **観測がゼロだと機能もゼロだと決めつけかけた**。`attempts=0` のデータでも、反実仮想では緩和で weighted が改善する（緩和は下限割れの罰も外すので別経路が効く）（3.326.0） | 観測は**その観測点で見えたことだけ**を語る。「0 件だから効かない」は別の経路を否定していない |
| 12 | **「意味論を直す」つもりで挙動を変えかけた**。Block Swap の評価枠を pass ごと→呼び出しごとへ直したら、pass 境界での候補再生成が減って real の採用手が 2→1 に落ちた。実効値を保つつもりで枠を 96 にしても、旧実装とは探索経路が違うので採用は戻らなかった（3.327.0） | **文書と実装が食い違うときは「どちらを直すか」を測ってから決める**。改善が無いのに挙動を変えない。今回は実装を残して**文書側を実装に合わせた** |
| 13 | **出口を1つ塞いで直したつもりになった**。3.324.0 で「採用しなかった盤面の診断は保存しない」を ViewModel に入れたが、**エンジン側の出口（`handleOptimize` の戻り値）は塞いでいなかった**。同型で、盤面ハッシュの自己失効を入れたのに、あとから「盤面を変えずに制約だけ変える」導線を自分で足して迂回路を作った（3.322.0/3.324.0/3.326.0→3.327.0） | 不変条件を入れたら**その値が通る経路を全部数える**。あとで新しい書き込み・変更の導線を足すときは、既存の不変条件がその経路でも成り立つか確かめる |
| 14 | **1つの旗で2つの状態を表していた**。`running` が「短い違反チェック」と「長い最適化」を兼ね、検査の完了が最適化の実行中を解除して、その旗を見ている全ガードが一斉に外れた（3.328.0） | 旗の名前が状態を1つしか指していないのに、書き込む側が複数あるなら疑う。**「この旗を false にしてよいのは誰か」を数える** |
| 15 | **ガードを画面ごとに配って毎回取りこぼした**。セル編集(3.161.0)・一括シート(3.127.0)・技能/制約/土台の編集(3.328.0)と、同じ穴を3回踏んだ | ガードは**全員が必ず通る入口**に置く。画面の数だけ置くやり方は、画面が増えるたびに漏れる |
| 16 | **テストの前提を実装で確かめずに書いた**。全角数字「２」を「非数値」のつもりで使ったが `toIntOrNull` は 2 として解釈する。実行して初めて判明（3.328.0） | 「この値はこう解釈されるはず」も**前提**。盤面と同じで、テストに書く前に実行して確かめる |
| 17 | **UI 層で「型のフィールド名」を確かめずに書いた**。`stateKey` が `C3Row.seq`・`C42Row.g1` を参照したが実際は `.pattern`・`.g1Kigou`。括弧均衡もスコープ逆引きも通り、CI で初めて落ちた（3.328.0→3.328.1） | UI 層でモデルの**プロパティを新しく参照**したら、**モデル定義と機械照合**する（`data class` から名前を抜いて突き合わせる）。ホストでコンパイルできない層では、これが唯一の事前チェック |
| 18 | **「既存を全置換する取込」で、読めない行を黙って捨てていた**。1行でも有効なら実行するので、誤記だらけのCSVが既存データを消せた（3.329.0） | 置換と追記を**取込ごとに区別**する。置換なら「全部読めたときだけ実行」。捨てた行は必ず数えて返す |
| 19 | **同じ決め打ちが1ファイル内で混在していた**。`Ws1Ops` は `addGroup` で休を記号解決しているのに、盤面を埋める3箇所は index 0 直書き（コメントも「休/idx0」と同一視）（3.329.0） | 正しい解決子が既にあるなら、**同じ概念を扱う箇所を全部 grep して揃える**。コメントが2つの概念を「/」でつないでいたら、たいてい取り違えている |
| 20 | **テストできない層に、テストが要るロジックを置いた**。入力の指紋（手書きで20以上のフィールドを畳む純関数）を ViewModel に書き、1つ書き忘れても誰も気づけない状態にした（3.328.0→3.330.0） | 純関数なら**テストできる層へ置く**。「UI 層だからテストできない」の多くは、置き場所を選び直せば解ける。実機が要るもの（時間差・並行）とは切り分ける |
| 21 | **繰り返し実行する処理の観測を、最後の回で上書きした**。後処理は C1研磨を複数巡回すのに `c1Plateau = it` としており、2巡目以降は1巡目が直したあとの盤面を見るので観測が少なく、説明が3/7箇所に減り件数も1/5になっていた（3.331.0） | ループの中で診断・統計を持ち帰るときは**上書きか合算かを決めてから書く**。「計測した回数」と名乗る値は合算でなければ名前と合わない |
| 22 | **`A && B` の安い方を後ろに置いていた**。結合探索が毎回フル checker を呼んでから安いピン検査をしており、ピン破りの組合せにも全額払っていた（3.331.0） | `&&` の評価順は**コストで決める**。採否が同一なら、安くて棄却率の高い条件を先に置く |
| 23 | **別の母集団の数字を1行に並べた**。`圧縮elite=10 相異なるelite=10 距離=0..88` は前2つがアーカイブ、距離が8ワーカーの話で、読むと矛盾に見えた。しかも中央の数字は恒真値で、意味のある信号（ワーカー解の潰れ）を打ち消していた（3.332.0） | 1行に並べる数字は**同じ母集団か確かめる**。恒真な数字は出さない（情報がないだけでなく、隣の数字の解釈を誤らせる） |
| 24 | **ログの矛盾を実装のバグと決めつけかけた**。C3nPolish の「パターン全域」表記をトグル無視のラベルバグと疑ったが、そのパスは元から常に全域を見ており表示は正しかった（3.332.0） | ログの違和感は**まず読み方を疑う**。同じ語がUIトグルと別の経路を指していることがある |
| 25 | **状態の解放を、自分で手放したあとに「まだ持っているか」で判定した**。`finally { if (ownsFiles()) 解放 }` は所有権をファイルから読み直すので、**正常終了パス（マーカーを先に消す）が自分を他人と判定して解放を飛ばし**、実行中フラグが永久に立ちっぱなしになった（3.329.0→3.333.0） | 「自分が持っているか」を**外部の状態から読み直す判定は、自分で手放した後には使えない**。手放したことを覚える旗を別に持つ。所有権を入れたら**正常終了・キャンセル・例外の3経路すべてを通す** |
| 26 | **族を網羅したテストで「構造」を守ったつもりになった**。指紋テストは入力29族を1つずつ変えて衝突まで見ていたが、**行の形（可変長の行の切れ目）を一度も変えていなかった**ので、値の並びが同じで構造だけ違う入力の衝突を通した（3.330.0→3.333.0） | 可変長の入れ子を畳む関数は、**値の変化と形の変化を別々にテストする**。「全部の族を試した」は「全部の壊し方を試した」ではない |
| 27 | **「規約を守っているか」を層ごとに確かめていなかった**。希望固定セルを触らない規約を、研磨の全パスと C++ の修復は守っていたのに SA/LAHC の近傍だけ守っておらず、**手の35%が採点でほぼ必ず却下される手**に消えていた。採点は正しいので出力は壊れず、誰も気づかなかった（3.334.0） | 同じ規約に従うべき層が複数あるなら、**規約ごとに全層を grep して数える**。「出力が正しい」は「無駄がない」を意味しない |
| 28 | **同じ判定を「委譲」でなく「複製」した**。`betterReport` は hard→weighted→total の3キーなのに、`V6LateOperators` の2つのゲートは第2キーまでを手書きで写しており第3キーへ落ちない。3.287.0（第2キーを寄せた回）と 3.309.0（hard を足した回）の**2回とも複製した**ので、2回とも同じ穴が残った（3.335.0） | 共有の判定関数があるなら**条件式を写さず呼ぶ**。写した瞬間、次に本体が変わったときそこだけ取り残される |
| 29 | **回避策を入れて設計の脆さを見送った**。`optimize()` が入口で可変 static を消すせいで「他の案」が失われるのを、退避→復元で塞いでいた。回避策が要ること自体が「実行ごとの持ち物になっていない」証拠だったのに、そのまま数版放置した（3.335.0） | 回避策を書くときは**なぜ要るのか**を1文で書く。「〜が消えるから退避する」と書けたら、消える設計のほうを直せないか先に考える |
| 30 | **バグを戻しても通るテストを書いて「固定した」と思った**。`strongPerturbFlat` の唯一の呼出元はネイティブ経路で、ホスト JVM は `.so` を読めないので走らない。間接テストは常に緑だった（3.336.0） | 新しいテストは**バグを戻して落ちることを確かめてから**採用する。落ちないなら、そのテストは何も守っていない |
| 31 | **同じ層の同じ規約を、片方だけ直して「完了」と宣言した**。3.334.0 で SA/LAHC の近傍4種にピンガードを入れたが、ネイティブ経路の停滞脱出 `strongPerturbFlat` を取り残した（3.334.0→3.336.0） | 規約を入れたら**その規約が要る関数を grep で数え、全部に入ったことを数字で確認**する。「主要な4つに入れた」は「全部に入れた」ではない |
| 32 | **例外ゲートの補助条件が、目的関数と逆向きだった**。c1 ブーストの `lim = 200×high + 120×low` は目的関数（high45 < low90）と大小が逆で、high−1/low+1 の入れ替えが lim を下げつつ weighted を悪化させられた（3.336.0） | 目的関数の外に**独自の重みを持つ補助式**を書いたら、目的関数と符号・順序が一致するか検算する。一致しないなら目的関数そのものをガードに足す |
| 33 | **「同じ意味を2箇所に書いている」と分かっていたのに、突き合わせる試験を作っていなかった**。C++ 側は CI で守っていたのに、Kotlin の Checker と Evaluator の間は目視とコメントだけで、実際に3回ドリフトしていた（3.337.0） | 二重管理を許すなら**その場で等式を試験にする**。「気をつける」「コメントに書く」は3回とも効かなかった。族を使う試験は**全族が発火したことを数字で確認**する（発火しない族は守られていない） |
| 34 | **「ガードが守っている」と思っていたが、実際に守っていたのは採否だった**。研磨の `movable` を1箇所潰しても試験は落ちない（希望を破ると pref 9000 が増え `isBetter` が必ず却下する）。全部潰して初めて落ちる（3.338.0） | 不変条件の試験を書いたら**何を壊すと落ちるか**まで実測する。「不変条件を守る試験」と「ガードの網羅を守る試験」は別物で、後者は grep で数えるしかない |
| 35 | **反復探索の途中で見つけた最良を捨てていた**。ビームは各ステップで全メンバーを子に置き換えるのに、返すのは最終ビームの最小だけ。実データで **maxSteps を増やすほど結果が悪くなった**（4で weighted 2859 → 6以降は根の 2985 に戻り採用0）（3.340.0） | 集団を丸ごと置き換える探索では「最後の状態が最良」は成り立たない。**最良を明示的に保持**し、**ステップ数を増やして単調非悪化かをテストで固定**する |
| 36 | **「近傍が消えるから悪化するはず」と予想して測ったら逆だった**。複合手の原子拒否は golden で `opBlockFill` を完全に no-op にするのに、品質は中立〜改善（real は HARD 中央 5→4）。見送りは `applyCell` を呼ばないので1反復が極端に安く、反復が2.2〜2.9倍回る（3.341.0） | 「機能が減る＝悪化」は直感であって根拠ではない。**手の数でなく、その手が意図を果たしているか**で見る。部分適用された複合手は意図を果たさないまま盤面を大きく壊していた |
| 37 | **兄弟パスの実行時間の差を「規約の非対称」と疑った**。C1共同LNS が毎回8秒使い切るのに個人共同LNS は1秒で終わるのを見て「片方に停滞打ち切りが無い」と考えたが、実際は**どちらも候補が尽きるまで走る同じ構造**で、C1 は候補を7万件作れるので尽きないだけだった（3.342.0） | 差を見たら**まず同じ理由で説明がつくか**を確かめる。3.331.0 以降「非対称を疑う」が効いてきたぶん、逆に何でも非対称に見える。数字（候補件数）を出せば一発で分かる |
| 38 | **同じ規律を1つの関数の全分岐へ通していなかった**。3.311.0 で「希望を破る代金を HARD の件数として勘定する」を PINNED 判定へ入れたが、隣の ADJACENT 分岐に入れ忘れ、**希望固定セルを「崩せる」と誤主張**していた（3.343.0） | 判定関数に新しい勘定を足したら、**その関数の全分岐**へ通す。#31 は「層をまたぐ網羅」、これは**1関数の中の網羅**。分岐ごとに `return`/`else if` が並ぶ関数はとくに漏れやすい |
| 39 | **直したものの「実害」を確かめずに書いた**。3.343.0 で「診断の誤りのせいで停滞打ち切りが発火しない」と書いたが、実際は covU 側の条件が先に効いていて診断は原因ではなかった（3.344.0 で自己訂正） | 修正の効果は**直した直後に測る**。「これで○○も直るはず」は仮説であって結果ではない。#9 の「仮説を測る前に信じない」は、自分の修正の波及にも当てはまる |
| 40 | **「この既定は効いているはず」と思い込んで撤去をためらった**。休を優先する既定2箇所（残り埋めのボーナス・代替候補の先頭）は、撤去すると品質が落ちると考えていたが、測ったら**3データセットとも他族が完全に同一**で最終盤面までバイト一致だった（3.345.0） | 「効いているはず」も仮説。**撤去して測る（ablation）**のは新機能を測るより安い。3.317.0（寄与ゼロを実測して撤去）と同じ手順が、今回は「撤去してよい」の根拠になった |
| 41 | **テストが「機構」でなく「その盤面での到達値」を固定していた**。`apt=0 まで解消`は、同じ重み1の weekly が強くなった瞬間に成立しなくなった（目的関数の最適が apt=0 と一致しない）（3.345.0） | 固定するのは**そのテストが守りたい機構**（ここでは「1パス内で反復する」＝`applied>1`）。到達値は他の族との釣り合いで動くので、**別の族の重み・定義が変わると壊れる**。壊れたら値を更新するのでなく、何を守りたかったかへ戻す |
| 42 | **単調でない条件から、単調なラッチを立てた**。停滞判定の `shouldStop` は改善が届けば偽に戻るのに、`stagnationFired` は一度立つと降りない。結果、探索が予算を使い切ったのにログは「早期終了」と書き、その古いラッチを根拠に追加精製まで skip されていた。同じ条件を `while` の継続判定に使う適応ポートフォリオでは、**一瞬 true の瞬間にポーリングしたワーカーだけが恒久的に離脱**し、8本中4本が115秒で消えて残り159秒を半分の並列度で走っていた（3.346.0） | 条件を旗に写すときは**その条件が単調かを確かめる**。単調でないなら旗も降ろす。単調でない条件を「一度きりの離脱判定」に使うと、**観測点の運だけで参加者が減る**（本人は止まったつもりがない）。片肺運転は合計値には現れないので、**参加者ごとの実消費を1行に出す**まで気づけない。逆に、単調でない条件のために入れた猶予を**単調な条件にも当ててはいけない**（確認窓を探索締切にも当てて後処理予約を5秒食った＝実測で捕捉） |
| 43 | **同じガードの意味を、隣り合う2行で違うものにしていた**。`pinBlocks.record` は `pinBad && isBetter(...)` と絞るのに、次の行の `rejectCulprits.record(rep, best, pinBad)` は生の pinBad を渡しており、**採点でも落ちる手までピン破りに数えて本当の主因族を隠していた**。実データで 98〜100% が誤ラベル（golden fair 28/28・user fair 266/266）。3.326.0 で片方だけ厳密化した取り残し（3.347.0） | 同じ語（ここでは「ピン破り」）を使う集計が複数あるなら、**その語が成り立つ条件を1文で書き、全部の呼出がその条件で絞っているか grep で数える**。隣の行が正しくても、自分の行が正しいとは限らない |
| 44 | **オラクルが使えない分岐を「テストできない」と諦めかけた**。T>64 のスカラー退避はビット経路と比較できない（ビットが使えない領域）ので無検査のままだった。65日目に「どのパターンの末尾にもならない値」を置くと境界の窓が必ず不成立になり、**先頭64日のビット結果と厳密に一致する**形で比較可能になった（3.348.0） | 比較対象が無い分岐は、**入力を制約して比較可能にできないか**先に考える。境界1つを無害化するだけで既存のオラクルが使えるようになることがある |
| 45 | **落ちないテストを「テストが弱い」とだけ読んだ**。`breakableDaysFor` の窓口迂回に「`1L shl j` が mod 64 で折り返す」と主張してガードを入れたが、範囲外の日で空を返すテストは修正を戻しても通った。読み直すと `coveringRunDaysAfterSet` が入口で `j` を弾いており、**折り返す経路は旧実装にも無かった＝主張そのものが偽**だった（3.469.0） | 教訓#30 で落ちなかったとき、まず疑うのは**テストでなく前提**。「テストが弱いから書き直す」の前に「その不具合は本当に存在するか」をコードで確かめる。落ちないテストは、**弱いテストのサインであると同時に、偽の主張のサインでもある** |

---

## 2. 作る前にやめた判断（同じ提案が来たらここを読む）

- **C1 の証明つき頭打ち診断（`C1FocusProof`）**：厳密窓探索が「探索し尽くした」に到達した窓は
  3データセット・全217窓で **0件**。作れば常に UNKNOWN_BUDGET を返す＝何も言わない診断になる（3.323.0）。
- **関与職員上限の引き上げ・選抜制**：2.4〜2.8倍遅く品質は同値か悪化／絞っても母数がほとんど減らない（3.314.0/3.315.0）。
- **早期終了で余った予算を soft 研磨へ回す**：提案の前提（c3n 壁の証明後に時間が余る）が実データで
  成立せず、実際に余るのは hard=0 到達時。その余りを予約枠ぶん使う穏当版は **+17秒で中立**（5回中2回しか
  現行中央を上回らず基準未達）。早期終了そのものの撤去は改善方向だが有意でなく（p≈0.075）、
  品質と電池の交換＝業務判断（3.341.1）。
- **covU-blocked をウォッチドッグへ配線して早期終了**：早期終了は focus 変更と違い **keep-best-safe でない**
  （keep-best は「見た解の最良」を保証するが、探索を止めると**まだ見ていない改善**は諦める＝回収不能）。
  「原理で採用」の例外は keep-best-safe な focus 並べ替えだけ。sample_v6 が「22秒停滞後に soft がバースト改善
  する」実在を示し、動機データ（real/user）消失＋golden は hard=0 で no-op のため直接 A/B 不可＝却下し revert（3.361.0）。

判断の型：**実装より先に「その機能が発火する条件が実データで成立するか」を測る**。
成立しないなら作らない。3.290.0 の「候補4件で採用0の不活性パス」を出したのが元の失敗。
否決はコードを残さず、根拠つきで `docs/algorithm_portfolio.md` の「実測で否決した提案」へ書く。

---

## 3. 測り方

- **1回の計測で A/B を判定しない。** 3回以上回して分布で見る。JointLNS は壁時計予算で動くので
  run 間に幅が出る（real の既知帯 49213〜49232）。同一 JVM 内の実行順でも変わる。
- **A/B は同一スクリプト内の連続実行で比べる。** 「決定的」と思っていた golden も、機械の負荷が変われば
  2469→2490 と動く（壁時計予算の JointLNS 由来）。別々のタイミングで測った2つの数字を並べて
  「改善した／退化した」と言わない。3.340.0 は同一スクリプト内の連続 A/B で3件とも一致を確認した。
- **時間を使っている場所と、成果を出している場所は別。** 3.339.0 で「後処理の27〜42%を占める」と分かった
  パスを、削る前に**何にその時間を使っているか**測ったら、8回の呼出すべてで最後の改善後に
  32〜60ステップが空回りしており、さらに最良を保持せず捨てていた（3.340.0）。
  一方で ablation では実際に効いていた＝**遅い＝無駄ではない**。削るか直すかは ablation で決める。
- **`weighted` を見る。`c1` 単体で判断しない。** 目的関数は hard → weightedScore → total の順で、
  c1 2件を手放して low(90) 5件を消す取引は正しい改善。族の件数だけ見ると悪化に見える。
  （重みの数値はここに書かない＝HF77 で変わると取り残される。現在値は `docs/business-logic.md` の重み表。）
- **前提の確認と効果の確認は別物。** 「focus が一度も選ばれていない（0/20）」が鮮やかに確認できても、
  下流が同じ情報を別経路で持っていれば直しても効かない（3.310.1 で実測）。
- **反実仮想は出荷せずに測れる。** データを書き換えず、緩めた版で研磨だけ回して差を見る。
- **「余っている資源を使えば得」も直感。** 予算の54%が余っているのを見て追加精製へ回したが、
  +17秒で中立だった（3.341.1）。資源が余っていることと、それを使って改善できることは別。
- **有意でない差を「改善」と書かない。** 早期終了の撤去は中央 −3.5% だが U検定 p≈0.075。
  分布の幅（326）に対しサンプル5では示せない。示せなかったことを結論として書く。

---

## 4. この環境の検証手段と穴

| 層 | 検証できるか | 手段 |
|---|---|---|
| v6 エンジン・model | **できる** | ホスト JVM で実コンパイル＋JUnit 実行（Gradle 同梱の `kotlin-compiler-embeddable`）。実データ3件で最終盤面の不変も確認。**バグ注入で検証するときはハーネスの第1引数にそのツリーを渡す**（既定は本体を見るため空振りする） |
| C++ / JNI | **できる** | `tools/native/host_parity_bench.cpp` を g++ で実ビルド・実行（mismatch=0）。CI にも配線済み |
| UI（Compose）・ViewModel・WorkManager | **できない** | 括弧均衡・シンボルのスコープ逆引き・呼び出し側シグネチャの静的確認まで。**最終判定は CI**。実行の所有権・kill 復元のような並行性はエミュレータ/実機でしか確かめられない |

**穴はここ**：UI 層は静的チェックが通ってもコンパイルが通る保証がない（#1・#17 がその実例）。
括弧均衡とスコープ逆引きは**構造**しか見ない。**名前**（プロパティ・関数のシグネチャ）は別途モデル定義と
突き合わせる。
機械的な一括編集をしたら、**エンジン層は必ずコンパイルまで通してから次へ進む**（型の不整合・引数の重複は
コンパイラだけが見つける。3.326.0 は18関数への一括配線で「引数を二重に渡した」等を実際にコンパイルで捕まえた）。
CI を起動したら**結果を確認するまで次へ進まない**。「CI 待ち」と書いて別の作業を始めた結果、
失敗に気づくのが1ターン遅れた。

**検証器そのものを疑う**（3.409.4 で実際に踏んだ）。教訓#30（防具が本当に発火するかバグを注入して確かめる）を
実行するとき、`hosttest.sh` はリポジトリルートを**直書き**していたため、scratch へコピーしてバグを入れても
**本体をコンパイルしていた**＝「0 failures」が出て検証が空振りした。しかも**空振りは成功と見分けがつかない**。
ハーネスを第1引数でルート差し替え可能にして再実行したところ、注入したバグでちょうど1件が落ちた。
同型の失敗は `hosttest.sh` のコンパイルエラー検出（`^e: ` だけを見て `error:` を見逃し「Tests run: 0」を
成功と表示した・3.406.0）でも踏んでいる。**検証が「何も起きなかった」と言ったら、まず検証器が対象を
見ているかを確かめる。** 対照的に `design_lint.py` は自分の位置からルートを導くので scratch 検証が正しく効いた
＝**ツールは実行位置でなく自分の位置から対象を決める**のが正しい（3.409.1 で 5 ツールを同じ形へ配線した）。

---

## 5. 受け取ったレビューの扱い

- **鵜呑みにせず全件コード照合する。** 直近は 5/5・4/5・**6件中4件が既に修正済み**だった（レビュー対象の
  コミットが古いことが多い）。既修正・事実誤認・対象が古い、を毎回仕分けてから残りに取りかかる。
- **「実在する」と「今すぐ直す」は別。** 3.333.0 の SA/LAHC は非対称（研磨と C++ 修復は希望固定を見るのに
  探索の近傍は見ない）が実在したが、採点は正しく実害は反復の空振り。直すと近傍が変わる＝§2の規律で
  計測してから 3.334.0 で採用した（12/15 勝・反復 +19〜41%）。**指摘の事実・重大度・対処の緊急度を
  分けて答え、保留したものは次に必ず測る。**
- **提示されたファイルをそのまま適用しない。** 切断された作業コピー（MD5 一致の再送・末尾欠落・
  `TODO()` 混入・コンパイル不能な1文字）を実際に受け取っている。内容は「意図の記述」として読み、
  現行ファイルへ手で反映する。
- **成立しない指摘は根拠を示して不採用にする。** 「fallback elite の並びが hard→total」は
  3.287.0 の統一順どおりだった、など。

---

## 6. コンテキストの固定費と変動費（3.468.0 で測って優先順位が入れ替わった）

**「AIレビューをしやすくしたい」の具体的な困りごとがコンテキスト圧迫なら、まず固定費を測る。**
聞く前に「Godファイル（`V6HotfixPasses` 4,682行）の分割」を P3 に置いていたが、測ったら的が外れていた。

| | 大きさ | いつ効くか |
|---|---|---|
| `CLAUDE.md` | **422k-tok** | **毎ターン固定**（プロジェクトメモリは全文が入る） |
| `V6HotfixPasses.kt` | 93k-tok | そのファイルを読んだターンだけ |
| main の全 Kotlin | 648k-tok | — |

しかも `CLAUDE.md` の内訳は**恒常ルール 30k に対し版数付き作業記録が 9割**。
**固定費を毎ターン払いながら、変動費 93k のファイルを分割しようとしていた。**
分割してもレビュー時に読むのは元々関数単位（`sed -n` / `grep -A`）なので、読む範囲は変わらない
（「変更の局所化」という別の利点は残る＝目的が違う）。

対処は**索引化**＝本文を `docs/history/` へ移し、**原文見出しのまま**の索引だけ CLAUDE.md に残す（422k→202k）。
要旨へ削ると見出しの補足版数（3.301.1 / 3.308.1 / 3.322.0 等）が索引から消えてその版で引けなくなることを
実測してから全文へ切り替えた。**内容は1行も捨てない**（非空行の多重集合一致で検証）。

**リスクは「読まれなくなって再発すること」**。作業記録は「測って否決した案・同型のバグ・決定記録」の資産で、
実際 3.419.0 / 3.386.0 / 3.385.0 の記録が判断を決めた場面がある。CLAUDE.md 冒頭に
「同じ領域を触る前に必ず索引を検索する」と引き方・新記録の書き方を明記して緩和したが、
**仕組みでなく指示なので確実ではない**＝そこは正直に書く。

**一般化**: 「重い」と感じたものを測る前に直さない。固定費（毎ターン払う）と変動費（読んだときだけ）を
分けて数え、固定費から削る。§2 の「測ってから作る／作らない」をコンテキストにも当てる。
